import dotenv from 'dotenv'; dotenv.config(); import mongoose from 'mongoose'; mongoose.connect(process.env.MONGODB_URI).then(async () => { const count = await mongoose.connection.db.collection('recipes').countDocuments({'title.en': {$exists: true}}); console.log('Translated:', count); const total = await mongoose.connection.db.collection('recipes').countDocuments({}); console.log('Total:', total); process.exit(0); });
