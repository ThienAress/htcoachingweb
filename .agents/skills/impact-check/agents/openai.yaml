interface:
  display_name: "Impact Check"
  short_description: "Trace cross-layer change impact"
  default_prompt: "Use $impact-check to trace every producer, consumer, contract, and test affected by this change."

policy:
  allow_implicit_invocation: true
